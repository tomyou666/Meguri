services:
  runcomfy:
    build: .
    image: meguri-runcomfy:latest
    env_file:
      - path: .env
        required: false
    environment:
      RUNCOMFY_TOKEN: ${RUNCOMFY_TOKEN:-}
    volumes:
      - ./output:/output
